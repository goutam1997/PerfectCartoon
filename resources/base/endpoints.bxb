endpoints {
  action-endpoints {
    action-endpoint (search) {
      accepted-inputs (keyword, category, author)
      local-endpoint (search.js)
    }
    action-endpoint (StartSurvey) {
      accepted-inputs (name, like)
      local-endpoint (StartSurvey.js)
    }
  }
}
